"use client";
import {useEffect,useState} from "react";
import "./mapbox.css";
import "./live-assist";
type Turn="left"|"right"|"continue"; type Step={action:Turn;street:string;until:string;note?:string}; type Dir={id:"westbound"|"eastbound";label:string;origin:string;destination:string;steps:Step[]};
const directions:Dir[]=[
{id:"westbound",label:"Westbound",origin:"11th Street",destination:"Aksarben T.C.",steps:[
{action:"left",street:"Dodge",until:"16th Street"},{action:"left",street:"16th Street",until:"Howard"},{action:"right",street:"Howard",until:"St. Mary's",note:"Veer left onto St. Mary's, then continue to Leavenworth"},{action:"right",street:"Leavenworth",until:"60th Street"},{action:"left",street:"60th Street",until:"Pacific"},{action:"right",street:"Pacific",until:"67th Street"},{action:"left",street:"67th Street",until:"Pine"},{action:"right",street:"Pine",until:"Aksarben Drive"},{action:"left",street:"Aksarben Drive",until:"Mercy Road"},{action:"right",street:"Mercy Road",until:"Aksarben T.C."}]},
{id:"eastbound",label:"Eastbound",origin:"Aksarben T.C.",destination:"11th Street layover",steps:[
{action:"continue",street:"Mercy Road",until:"72nd Street"},{action:"right",street:"72nd Street",until:"Pine"},{action:"right",street:"Pine",until:"67th Street"},{action:"left",street:"67th Street",until:"Pacific"},{action:"right",street:"Pacific",until:"60th Street"},{action:"left",street:"60th Street",until:"Leavenworth"},{action:"right",street:"Leavenworth",until:"16th Street"},{action:"left",street:"16th Street",until:"Douglas"},{action:"right",street:"Douglas",until:"11th Street"},{action:"left",street:"11th Street",until:"Layover"}]}];
type Screen="home"|"route"|"learn"|"quiz"|"test"|"live"|"progress"; const arrow=(a:Turn)=>a==="left"?"↰":a==="right"?"↱":"↑"; const label=(a:Turn)=>a==="continue"?"Continue":`Turn ${a}`;
export default function Home(){const[screen,setScreen]=useState<Screen>("home"),[dirId,setDirId]=useState<Dir["id"]>("westbound"),[index,setIndex]=useState(0),[score,setScore]=useState({correct:0,attempts:0}),[feedback,setFeedback]=useState<null|"correct"|"wrong">(null),[started,setStarted]=useState(false),[live,setLive]=useState<"ready"|"tracking"|"off-route">("ready"),[speech,setSpeech]=useState(true);const dir=directions.find(d=>d.id===dirId)!;const step=dir.steps[index],next=dir.steps[index+1],pct=Math.round((index+1)/dir.steps.length*100);useEffect(()=>{const s=localStorage.getItem("rt-score");if(s)setScore(JSON.parse(s));const savedSpeech=localStorage.getItem("rt-speech");if(savedSpeech!==null)setSpeech(savedSpeech==="true");if("serviceWorker" in navigator)navigator.serviceWorker.register("/sw.js").catch(()=>{})},[]);const speak=(text:string)=>{if(!speech||!("speechSynthesis" in window))return;window.speechSynthesis.cancel();const utterance=new SpeechSynthesisUtterance(text);utterance.rate=.92;utterance.pitch=1;window.speechSynthesis.speak(utterance)};const announceStep=(i=index)=>{const s=dir.steps[i];speak(`${label(s.action)} on ${s.street}. Continue to ${s.until}.`)};const toggleSpeech=()=>{const enabled=!speech;setSpeech(enabled);localStorage.setItem("rt-speech",String(enabled));if(enabled){window.speechSynthesis?.cancel();const utterance=new SpeechSynthesisUtterance("Voice guidance on.");window.speechSynthesis?.speak(utterance)}else window.speechSynthesis?.cancel()};const go=(s:Screen)=>{setScreen(s);setIndex(0);setFeedback(null);setStarted(false);scrollTo(0,0)};const advance=()=>{setFeedback(null);setIndex(i=>{const n=i>=dir.steps.length-1?0:i+1;if(screen==="live")setTimeout(()=>announceStep(n),80);return n})};const answer=(a:Turn)=>{if(feedback)return;const n={correct:score.correct+(a===step.action?1:0),attempts:score.attempts+1};setScore(n);localStorage.setItem("rt-score",JSON.stringify(n));setFeedback(a===step.action?"correct":"wrong")};const mastery=score.attempts?Math.round(score.correct/score.attempts*100):0;
return <main className="shell"><header className="top"><button className="brand" onClick={()=>go("home")}><b>RT</b> Route Trainer</button><span className="streak">● 4 day streak</span><button className="avatar" onClick={()=>go("progress")}>JD</button></header>
{screen==="home"&&<section className="page home"><span className="eyebrow">PERSONAL DRIVER TRAINING</span><h1>Know the route.<br/><em>Own every turn.</em></h1><p className="lede">Build confidence one trip at a time. Learn, test, and practice your routes from anywhere.</p><div className="stats"><div><b>{mastery}%</b><small>Overall mastery</small></div><div><b>1</b><small>Route in training</small></div><div><b>{score.attempts-score.correct}</b><small>Turns to review</small></div></div><div className="section-title"><div><span className="eyebrow">YOUR LIBRARY</span><h2>My Routes</h2></div><button disabled>＋ Add route</button></div><article className="route-card" onClick={()=>go("route")}><div className="badge">11</div><div><span className="tag">IN TRAINING</span><h3>Leavenworth / Aksarben</h3><p>11th Street ↔ Aksarben Transit Center</p><div className="bar"><i style={{width:`${Math.max(18,mastery)}%`}}/></div><small>{score.attempts?`${mastery}% mastered`:"Ready to begin"}</small></div><strong>›</strong></article><button className="tip" onClick={()=>go("quiz")}><span>✦</span><div><b>Quick practice</b><small>Five minutes now makes the next drive easier.</small></div><strong>Start →</strong></button></section>}
{screen==="route"&&<section className="page"><button className="back" onClick={()=>go("home")}>← My Routes</button><div className="route-hero"><div className="big-badge">11</div><div><span className="eyebrow">OMAHA METRO</span><h1>Leavenworth<br/>/ Aksarben</h1><p>Choose a direction, then how you want to train.</p></div></div><div className="tabs">{directions.map(d=><button key={d.id} className={dirId===d.id?"active":""} onClick={()=>{setDirId(d.id);setIndex(0)}}><b>{d.label}</b><small>{d.origin} → {d.destination}</small></button>)}</div><div className="strip"><span>{dir.origin}</span><i/><b>{dir.steps.length} maneuvers</b><i/><span>{dir.destination}</span></div><div className="modes"><Mode icon="⌁" color="amber" title="Learn Route" text="Walk through every maneuver with hints" onClick={()=>go("learn")}/><Mode icon="?" color="blue" title="Turn Quiz" text="Choose left, right, or straight" onClick={()=>go("quiz")}/><Mode icon="✓" color="pink" title="Full Route Test" text="Complete the route from memory" onClick={()=>go("test")}/><Mode icon="⌖" color="green" title="Live Assist" text="GPS-ready guidance with drive simulator" onClick={()=>go("live")}/></div></section>}
{screen==="learn"&&<Trainer title="Learn Route" dir={dir} pct={pct} back={()=>go("route")}><div className="card maneuver"><div className="turn">{arrow(step.action)}</div><span className="eyebrow">STEP {index+1} OF {dir.steps.length}</span><h2>{label(step.action)} on<br/><em>{step.street}</em></h2><div className="continue"><small>CONTINUE TO</small><b>{step.until}</b></div>{step.note&&<p className="note">↳ {step.note}</p>}</div><div className="actions"><button disabled={!index} onClick={()=>setIndex(i=>Math.max(0,i-1))}>← Previous</button><button className="primary" onClick={advance}>{index===dir.steps.length-1?"Start again":"Next step →"}</button></div>{next&&<div className="peek"><small>UP NEXT</small><b>{arrow(next.action)} {label(next.action)} on {next.street}</b></div>}</Trainer>}
{(screen==="quiz"||screen==="test")&&<Trainer title={screen==="quiz"?"Turn Quiz":"Full Route Test"} dir={dir} pct={pct} back={()=>go("route")}>{screen==="test"&&!started?<div className="card start"><span className="mode-icon pink">✓</span><h2>Ready for the full route?</h2><p>No hints. Complete all {dir.steps.length} maneuvers from {dir.origin} to {dir.destination}.</p><button className="primary" onClick={()=>setStarted(true)}>Begin test</button></div>:<><div className="card question"><span className="eyebrow">YOU ARE ON {index?dir.steps[index-1].street.toUpperCase():dir.origin.toUpperCase()}</span><h2>Approaching <em>{step.street}</em>.<br/>What do you do?</h2><p>Next destination: {step.until}</p></div><div className="answers">{(["left","continue","right"] as Turn[]).map(a=><button key={a} className={feedback?(a===step.action?"correct":"dim"):""} onClick={()=>answer(a)}><span>{arrow(a)}</span><b>{a==="continue"?"Straight":a[0].toUpperCase()+a.slice(1)}</b></button>)}</div>{feedback&&<div className={`feedback ${feedback}`}><div><b>{feedback==="correct"?"That's it.":`Not quite — ${label(step.action)}.`}</b><small>Continue on {step.street} to {step.until}.</small></div><button onClick={advance}>Continue →</button></div>}</>}</Trainer>}
{screen==="live"&&<section className="live"><header><button onClick={()=>go("route")}>×</button><div><small>ROUTE 11 · {dir.label.toUpperCase()}</small><b>{dir.destination}</b></div><button className={`speech-toggle ${speech?"on":""}`} aria-label={speech?"Turn voice guidance off":"Turn voice guidance on"} onClick={toggleSpeech}>{speech?"🔊":"🔇"}</button></header><div className="map" data-direction={dirId}><div className="road one"/><div className="road two"/><div className="road three"/><div className="route-line"/><div className={`bus ${live}`}>11</div><span className="street a">Leavenworth</span><span className="street b">60th St</span><span className="street c">Pacific</span><label>ROUTE 11 · {dir.label.toUpperCase()}</label></div><div className="live-panel"><div className="live-status"><span className={live}>{live==="ready"?"GPS READY":live==="tracking"?"ON ROUTE":"OFF ROUTE"}</span><small>STEP {index+1} OF {dir.steps.length}</small></div><div className="voice-state"><span>{speech?"🔊 Voice guidance on":"🔇 Voice guidance off"}</span><button onClick={()=>announceStep()}>Hear instruction</button></div><div className="next"><span>{arrow(step.action)}</span><div><small>NEXT MANEUVER</small><h2>{label(step.action)} on {step.street}</h2><b>{live==="tracking"?"0.3 mi":"—"}</b></div></div>{next&&<div className="then">THEN <b>{arrow(next.action)} {label(next.action)} on {next.street}</b></div>}<button className="primary wide" onClick={()=>{if(live==="ready"){setLive("tracking");announceStep()}else advance()}}>{live==="ready"?"Start simulated drive":"Simulate next turn →"}</button><button className="ghost" onClick={()=>{const off=live!=="off-route";setLive(off?"off-route":"tracking");if(off)speak("You are off route. Return to Route 11 when safe.")}}>{live==="off-route"?"Return to route":"Simulate off-route"}</button></div></section>}
{screen==="progress"&&<section className="page"><button className="back" onClick={()=>go("home")}>← Dashboard</button><span className="eyebrow">YOUR TRAINING</span><h1>Progress</h1><div className="score"><div className="ring" style={{"--score":`${mastery}%`} as React.CSSProperties}><span>{mastery}%</span></div><div><h2>Route 11 mastery</h2><p>{score.attempts} practice answers · {score.correct} correct</p><button className="primary" onClick={()=>go("quiz")}>Practice trouble spots</button></div></div><h2 className="trouble-title">Trouble spots</h2><div className="empty"><span>{score.attempts-score.correct?"↰":"✓"}</span><b>{score.attempts-score.correct?`${score.attempts-score.correct} missed turn${score.attempts-score.correct===1?"":"s"}`:"No trouble spots yet"}</b><p>{score.attempts-score.correct?"Practice them again to raise your mastery.":"Complete a quiz and missed turns will show up here."}</p></div></section>}
{screen!=="live"&&<nav><button className={screen==="home"?"active":""} onClick={()=>go("home")}><span>⌂</span>Routes</button><button className={screen==="quiz"?"active":""} onClick={()=>go("quiz")}><span>◇</span>Practice</button><button className={screen==="progress"?"active":""} onClick={()=>go("progress")}><span>◔</span>Progress</button><button onClick={()=>alert("Use your browser's Add to Home Screen option to install Route Trainer.")}><span>⇩</span>Install</button></nav>}</main>}
function Mode({icon,color,title,text,onClick}:{icon:string;color:string;title:string;text:string;onClick:()=>void}){return <button onClick={onClick}><span className={`mode-icon ${color}`}>{icon}</span><div><b>{title}</b><small>{text}</small></div><strong>›</strong></button>}
function Trainer({title,dir,pct,back,children}:{title:string;dir:Dir;pct:number;back:()=>void;children:React.ReactNode}){return <section className="trainer"><header><button onClick={back}>←</button><div><small>ROUTE 11 · {dir.label.toUpperCase()}</small><b>{title}</b></div><strong>{pct}%</strong></header><div className="progress"><i style={{width:`${pct}%`}}/></div><div className="training-body">{children}</div></section>}

